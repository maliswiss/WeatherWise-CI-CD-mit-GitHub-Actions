# =====================================================
# WeatherWise · Beispiel-Umgebungsvariablen
# Keine echten Werte commiten – diese Datei dient nur
# als Vorlage. Die Pipeline benötigt zur Laufzeit keine
# zusätzlichen Secrets ausser GITHUB_TOKEN (built-in).
# =====================================================

# Frontend (zur Laufzeit nicht zwingend nötig)
VITE_API_BASE_URL=https://api.example.com
